import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const ImageDetail = ({text}) => {
    return <Text>Image {text}</Text>
};

const styles = StyleSheet.create({});

export default ImageDetail;