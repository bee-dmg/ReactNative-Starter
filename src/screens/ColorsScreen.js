import React from 'react';
import {View,Text,StyleSheet,Button} from 'react-native';

const ColorsScreen = () =>{
return <View><Button title="Add a Color" onPress={()=>{}}/></View>
};

const styles = StyleSheet.create({});

export default ColorsScreen;